* UI Update Notes - 2026-04-09
** Reference: pi-tool-display (MasuRii/pi-tool-display)

** Implemented (PR #11: yg/ui-updates)

*** 1. Diff Renderer — src/utils/diff-renderer.ts
- [X] Split (side-by-side) and unified diff views
- [X] Width-adaptive mode selection: split (>=120), unified (>=40), summary (<40)
- [X] Token-level LCS for word-level inline diff highlighting
- [X] True-color 24-bit RGB backgrounds (green/red tints, emphasis on changed tokens)
- [X] ANSI background stability: preserveBg rewrites full resets to foreground-only
- [X] Syntax highlighting via cli-highlight on diff lines
- [X] Language auto-detection from file extension (~30 languages)
- [X] ANSI-aware truncation (visibleLen for correct width measurement)
- [X] Change pairs: syntax on matched tokens, bold emphasis on changed tokens
- Follows render(width) -> string[] protocol

*** 2. Box Frame Component — src/utils/box-frame.ts
- [X] renderBoxFrame(content, opts) -> string[]
- [X] 4 border styles: rounded, square, double, heavy
- [X] Title in top border, footer section below divider
- [X] Configurable border color
- Follows render(width) -> string[] protocol

*** 3. Tool Display — src/utils/tool-display.ts
- [X] renderToolCall / renderToolResult -> string[]
- [X] 3 display modes: full (>=80), compact (>=40), summary (<40)
- [X] Spinner with elapsed timer ("3s", "2m 15s", etc.)
- [X] Quiet-command detection (cd, mkdir, git add, etc.)
- [X] Command string shown below tool title (e.g. "$ git status")
- [X] Command output truncation (30 lines max + "… N more lines")
- Follows render(width) -> string[] protocol

*** 4. Yolo Mode + Permission Prompts as Extension
- [X] Default to auto-approve for all tool calls and file writes
- [X] Removed interactive-prompts from built-in extensions
- [X] Moved to examples/extensions/interactive-prompts.ts
- [X] Users opt-in by copying to ~/.agent-shell/extensions/ or via -e flag

*** 5. Extension Loader Rework — src/extension-loader.ts
- [X] Three extension sources (merged, deduplicated):
  1. CLI: -e / --extensions (repeatable, comma-separated)
  2. Settings: ~/.agent-shell/settings.json → "extensions": [...]
  3. Directory: ~/.agent-shell/extensions/ (auto-discovered)
- [X] TypeScript extensions supported (.ts, .tsx, .mts) via tsx/esm/api
- [X] npm packages as extensions (bare specifier → Node resolution)
- [X] Directory extensions with index.{ts,js} entry points
- [X] -e shorthand for --extensions
- [X] tsx moved to runtime dependency

*** 6. TUI Renderer Updates — src/extensions/tui-renderer.ts
- [X] Uses renderToolCall/renderToolResult from tool-display.ts
- [X] Spinner delegated to tool-display.ts (with elapsed timer)
- [X] Captures agent:tool-call command string, passes to showToolCall
- [X] Command output truncated at 30 lines with overflow indicator
- [X] Inline diff preview: file writes rendered in bordered box with unified diff
- [X] Ctrl+O to expand truncated diffs (footer hint shown when truncated)
- [X] User message box: queries shown in cyan-bordered box with word wrapping
- [X] Thinking spinner: agent:thinking-chunk events trigger "Thinking" spinner

*** 7. Other Changes
- [X] Exported wrapLine from markdown.ts for reuse
- [X] Added package.json exports: ./utils/*, ./types
- [X] Updated README: yolo mode, new modules, extension system docs

** Component Protocol Status
- render(width) -> string[] adopted in: diff-renderer, box-frame, tool-display
- MarkdownRenderer still uses streaming push/flush model (different pattern, appropriate for streaming)

*** 8. Event Bus — src/event-bus.ts
- [X] Added agent:thinking-chunk event for thought/reasoning streams
- [X] Emitted from acp-client.ts on agent_thought_chunk session updates

*** 9. Color Palette — src/utils/palette.ts
- [X] ~10 semantic color slots (accent, success, warning, error, muted + bg variants + modifiers)
- [X] Singleton module: components import `palette as p` directly
- [X] setPalette(overrides) for theming, exposed on ExtensionContext
- [X] Migrated: tui-renderer, diff-renderer, tool-display, box-frame, interactive-prompts
- [X] Re-exported from core.ts for library consumers

** Not Yet Implemented (future work)
- [ ] Migrate remaining files to palette (input-handler, markdown, slash-commands, index)
- [ ] Theme extension example (e.g. solarized, monokai)
